#include "bst.hpp"
#include <map>

int main()
{
    Tree<int> t;
    t.root = t.insert(t.root,1);
    // root = insert(root,2);
    // root = insert(root,3);
    // root = insert(root,4);
    // root = delete_node(root,1);
    // std::cout << get_balance_factor(root) << std::endl;
    // postorder(root);
    // std::map<int,int> a;

    return 0;
}